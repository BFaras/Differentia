import { SocketTestHelper } from './socket-test-helper';

// quelque chose à faire???? rien dans le projet du prof

describe('SocketTestHelper', () => {
  it('should create an instance', () => {
    expect(new SocketTestHelper()).toBeTruthy();
  });
});
