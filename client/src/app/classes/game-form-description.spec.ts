import { GameFormDescription } from './game-form-description';

describe('GameFormDescription', () => {
  it('should create an instance', () => {
    expect(new GameFormDescription()).toBeTruthy();
  });
});
